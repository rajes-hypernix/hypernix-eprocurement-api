{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Demo": {
    "//": "X-Demo-User impersonation + dev-login/dev-users. Always active in Development; hard-blocked in Production regardless of this flag; opt-in only in other environments (e.g. Staging).",
    "Enabled": false
  }
}
