# Backlog

Deferred work registered at slice sign-offs. One row per item.

| Item | Origin | Target slice | Status |
|---|---|---|---|
| Expose MaxExtensions in RfqDetail DTO and delete the client MAX_EXTENSIONS constant in RfqGovernance.tsx | Slice J sign-off | next backend slice | ✅ Done — Hardening 1 (Quick-Wins), T9 |
| Vendor list filters (MyRfqs facet, invoice/delivery) — intentional enhancement beyond prototype, pending SPSB UAT feedback | Slice K Phase 0 | pending SPSB UAT | Open |
| DashboardService: 'RFQs to bid' count must exclude Declined invitations — one-line server fix | Slice K Phase 0 | next backend slice | ✅ Done — Hardening 1 (Quick-Wins), T8 |
| `GET /api/auth/personas` is demo-only (leaks the persona roster) but was left ungated in Hardening 1 (T1 gated dev-users/dev-login only) — the Slice F auth exemption-list test must decide its disposition | Hardening 1 (Quick-Wins), T1 | Slice F (auth exemption list) | ✅ Done — Slice F Phase 1: authenticated-only (NOT on the exemption list); asserted by the anonymous-sweep test |
| GRN receipt date uses a hardcoded `clock.UtcNow.AddHours(8)` Malaysia-timezone offset in `DeliveryService` — a smaller cousin of the string-date debt; fix with the typed-dates work, don't rediscover | Hardening 1 (Quick-Wins), T7 | Slice H (typed dates) | ✅ Done — Slice H T4: Grn.ReceivedDate is a typed DateOnly (Malaysia business date, Kuching UTC+8); the string+offset hack is gone |
| `StoredFile` has no `OwnerVendorId`/attachment linkage — file-download ownership is inferred by scanning `"<fileId>::<name>"` answer values + `OnboardingDocument.StoredFileId` (Slice F `FileAccessPolicy`, deny-on-uncertainty). Add a typed ownership column so Slice G fixes the schema instead of the inference calcifying | Slice F Phase 2 | Slice G (integrity) | ✅ Done — Slice G T5: OwnerKind/OwnerVendorId/OwnerEntityId + migration backfill; FileAccessPolicy reads the column |
| Replace personal email (vendor-invites@hypernix.test) in seed data, tests and docs with a neutral placeholder (vendor@example.test) — 14 files. Must land BEFORE the repo is handed to the partner's tech team; git history is permanent, so the scrub only helps if done before wide distribution. | Git init secrets sweep | before handover | Open |
| Git initialized + secrets swept + tagged v0.1-slice-f | Slice F close-out | ✅ Done |
| Add Invoice.GrnId (nullable) + FK — needed for three-way-match lineage | Slice G T1 | Slice H | ✅ Done — Slice H T3: Invoice.GrnId + FK→Grn; set on creation when the PO has one GRN, backfilled likewise (ambiguous/receipt-less → null) |
| PrLineSourcing→PR: no PrId column; PrLine is an owned entity so its key can't be FK'd. Resolve when PrLine gets a stable Guid key | Slice G T1 | Slice H | ✅ Done — Slice H T8: PrLineSourcings.PrLineId → PrLines.Id, a DEFERRABLE DB-level FK (EF models no nav to an owned entity), added after T1's stable PrLine.Id, with orphan detection |
| PR status is stringly-typed while every other aggregate uses an enum; RequisitionDto exposes both Status (string) and HeaderStatus (enum) — converge on the enum and remove the duplicate DTO field | Slice G T3 | Slice H or dedicated cleanup | Open |
| `Rfq.MarkAwarded()` and `Asn.MarkReceived()` set status with no precondition (behaviour-preserving: the pre-Slice-G services had no guard either). Consider tightening to require Closed/Evaluation (award) and InTransit (receipt) once verified against every reachable path | Slice G T3 | role-matrix / lifecycle pass | Open |
| Resolve MSB3277 EF Core Relational version conflict in eProcure.Tests (10.0.4 vs 10.0.9) — align package versions so a blanket -warnaserror becomes achievable | CI setup | cleanup | ✅ Done — A2F-T7 (9ab4e68): Npgsql 10.0.3 + explicit Relational 10.0.9; clean build 0 warnings |
| CI web job uses `npm ci --legacy-peer-deps` because openapi-typescript@7.13.0 pins peer typescript@^5 while the app is on TS 6. Dev-only codegen tool, unused in build/lint/test. Retire the flag when openapi-typescript supports TS 6, or replace the tool. | CI setup | cleanup | ✅ Done — A2F-T7 (9ab4e68): openapi-typescript left package.json (gen:api via npx@7.13.0 — no TS6 release exists) + oxlint-tsgolint ^0.24; plain npm ci proven, flag dropped from ci.yml |
| Bump actions/checkout, setup-node, setup-dotnet to @v5 (Node 20 deprecation annotation) | CI setup | cleanup | Open |
| Model `VendorCertification` validity as a proper typed date. T4 left `ValidTo` a string because the data is bare years/placeholders ("2027", "—"), not dd/MM/yyyy — it can't be typed without fabricating a month/day. Root cause: the certification-entry UI accepts free text. The modelling fix (likely a full expiry `DateOnly`, needed by expiring-certs alerting and Phase-2 supplier scorecards) must land TOGETHER with the capture-form change and a vendor-confirmed backfill of real expiry dates — not as a typing change alone. | Slice H T4 | product decision + capture-form change | Open |
| On-time-delivery (Otd) is null in the derived VendorPerformanceView: there is no promised/expected delivery date on PO or PO line to measure `Grn.ReceivedDate` against. Add a promised-delivery-date column (PO or PO line) so OTD becomes computable. Feeds Phase-2 supplier scorecards. | Slice H T7 | scorecard / P2P enhancement | Open |
| Compliance breaches is null in the derived VendorPerformanceView: a short receipt (`GrnLine.Condition='Short'`) is a delivery shortfall, not a modelled compliance breach (a policy/contract/audit violation the system doesn't capture). Introduce a real breach register — plausibly a Phase-2 supplier-scorecard input alongside the annual vendor evaluation — then compute Breaches from it. | Slice H T7 | scorecard / vendor evaluation | Open |
| `Bid.Lead` (the vendor's self-reported lead time at bid) is now unused by the Lead metric (T7 computes actual PO-issued→goods-received days instead). Keep it for a future promised-vs-actual lead-variance metric ("quoted 2 weeks, delivered in 4") — arguably the strongest scorecard signal; it joins the promised-delivery-date row above. | Slice H T7 | scorecard | Open |
| Migrate ManualVendorForm's local Field/Select to the ui/ primitives (FieldSpec pipeline) — the second of the three rival Field components; PrForm's died at D1 Phase 3 | D1 Phase 3 | D2 window (or when touched) | ✅ Done — D2 Phase 3a |
| Migrate OnboardingForm's local Field/Select to the ui/ primitives — the third rival; its Section/Stat locals join the D2 archetype consolidation | D1 Phase 3 | D2 window (or when touched) | ✅ Done — D2 Phase 3d (Field/Select/Stat; local Section stays until its screen's gate) |
| Chat MessageInput (Enter-to-send input + send button, duplicated in ChatDock + Clarifications) — share ONE comm component when touched; a composition, not a FieldSpec primitive (D0 misfit F6) | D0 inventory | comm surface touch | ✅ Done — A2F-T7 (9ab4e68): comm/MessageInput.tsx, markup byte-preserved per host; raw-elements grandfather regenerated (103→102) |
| Bulk row-select + indeterminate select-all (Requisitions) — a List-archetype concern, not a field; absorb into the D2 table archetype (D0 misfit F31) | D0 inventory | D2 (List archetype) | ✅ Done — D2 Phase 1c: ListPage bulkSelect (Requisitions adopts at its own gate) |
| Near-duplicate green inks: --green-ink #256b41 (pills/chips) vs --green-ink-alt #2c6b46 (ribbon-success/health-ok) — converge to one token if design agrees; kept verbatim at D1 per no-restyle | D1 Phase 1 | product/design decision | Open |
| Notice tone `info` renders the base gradient ribbon while error/warn/success have flat tone washes — consider a --blue-bg info wash for consistency once design reviews the D1 token set | D1 Phase 1 | product/design decision | Open |
| Recents server-side persistence — D2 ships localStorage per principal (capped 8); move to D4's personalization tables so the trail follows the user across browsers | D2 Phase 2 | D4 (personalization) | Open |
| Top center-tab shell (NetSuite-style) as an ALTERNATIVE to the sidebar — nav data is already in center-tab config shape (centerTabs.ts); switching the renderer is an OPERATOR design decision requiring a mockup and explicit ruling (D2 Step 0 ruling: sidebar stays) | D2 Step 0 | operator design decision | Open |
| Requisitions' hand-rolled bulk-select + line-drawer migrate onto ListPage/archetypes at its own gate (largest remaining non-composing page). At that migration, design the quick filters AS saved-view criteria from the start — the facet→criteria mapping was deliberately deferred on RfqList (D3 ruling 3: layered for e2e parity), not bolted on. **D7.5 annotation (ruled):** the ViewPicker now mounts LAYERED via ID-INTERSECTION (a picked view filters the typed rows by the run's Id set at the 200-row paging cap — the accepted limit, noted in code); the migration inherits BOTH obligations: facets-as-criteria from the start AND retiring the intersection/cap for a real projected list | D2 Phase 4 + D3 ruling 3 + D7.5 | Requisitions gate | Open |
| Saved-view aggregation seam — the run endpoint returns rows only; D4 portlets/KPIs need count/sum over a view. Extension point documented in SavedViewService (typed IQueryable sources WITH their scoping); build at D4, not before | D3 (ruled) | D4 (dashboards/portlets) | Open |
| Vendor-portal saved-view UI exposure — vendors hold UseSavedViews/ManageOwnSavedViews and the executor scopes them safely (gate-proven via API), but the portal has no picker/builder surface yet | D3 Phase 3 | vendor-portal touch | Open |
| Slice F patch: vendor-principal scoping on RFQ detail read — confirmed gap (a vendor with no invitation can read any RFQ detail incl. invited-vendor names and events, RfqService.GetAsync); fix as a standalone micro-commit immediately after RM lands. The GRN-for-ASN read (DeliveryService.GetGrnForAsnAsync, same resource-scoping class) rides the same micro-commit | AUTHORIZATION-MATRIX Obs-1 + Obs-4 | next (immediately after RM) | ✅ Done — RM-P1: live-invitation guard on RfqService.GetAsync + EnsureCanAccess on GetGrnForAsnAsync; RfqGrnScopingTests (403 convention, rescinded ≠ live) |
| Onboarding magic-link page depends on authenticated /swec (and custom-list lookups via field options) — breaks in any non-demo environment (works today only because the demo client always sends X-Demo-User). Design a token-scoped or anonymous read-only lookups endpoint for the onboarding surface | AUTHORIZATION-MATRIX Obs-6 | before staging deploy | ✅ Done — A2F-T2 (f4e727a): POST /onboarding/lookups, token-verified, payload capped to the 4 form lists + SWEC; both sweep lists 12 |
| SetScoreAsync validates the assignment of req.EvaluatorId (request body) rather than the authenticated caller — an assigned evaluator's score can be written by any principal holding ScoreTechnical naming them. Bind the score to the caller (or assert caller == EvaluatorId) | AUTHORIZATION-MATRIX Obs-7 | hardening pass | Open |
| Chart forecast/projection (the demo mockup's dashed forecast) — deterministic regression on chart portlets as a PRODUCT-ROADMAP item; D4 ships actuals only (ruled) | D4 Step 0(b) | product roadmap | Open |
| spendVsSameMonthLy renders "not yet available" until 13 months of invoice history exist — self-activates with real data (Slice H posture, verbatim). Same for vendors-onboarded charts (all seed vendors share one CreatedUtc) and prToPoCycleDays (IssuedUtc backfill-null) | D4 Phase 2 | self-activates | Open |
| "Open requisitions" metric counts ALL PRs including cancelled/sourced — label lies. Fix label→query alignment as an operator-ruled product correction post-D4 (OD-D4-1: the parity gate deliberately did NOT fold in a numeric change) | D4 OD-D4-1 | post-D4 correction | Open |
| Custom field DataTypes deferred: RecordRef (first honest consumer = L4 custom records) and DateTime (all user-entered business dates are DateOnly per Slice H; instants are system-stamped) — add as enum members + sparse columns when a consumer exists | D5 Step 0(a) ruled | L4 / consumer-driven | Open |
| Reporting SQL views per record type (flattened custom-field columns for external analytics) EXPLICITLY deferred: no consumer until the NetSuite/external-analytics era, and the registry path already serves saved views + KPIs. Revisit when an external BI/ERP reader appears. Note: for DIMENSION needs (spend category etc.), D6 segments — not fields — are the answer | D5 Phase 4 | NetSuite/analytics era | Open |
| **PR dimension convergence (the D6 (iii-a) rationale, recorded):** Department/Location/Category/Job + Project live as PR COLUMNS with FREE-TEXT entry (PrForm text inputs — labels like "Bintulu Plant"/"bintulu plant" mint separate dimension keys today; the backfill found only clean values because seeds are clean). The four system segments mirror the columns one-way; converging the PR onto segment-backed selects (killing free-text entry, retiring the columns or demoting them to projections, folding PR.Project into the demo-proven user-segment path) is ONE deliberate migration slice with its own gate. Until then: columns = single truth, projection + PG probe = the drift guard | D6 Step 0(a) ruled (iii-a) | dedicated convergence slice | Open |
| Segment hierarchy ROLLUP reporting (group-by a parent aggregates its children) — ParentValueId is stored flat from day one (D6 ruled (c)); the rollup query/UX ships when a real reporting need names it | D6 Step 0(c) ruled | gate-driven | Open |
| Stacked view-backed CHART portlet (series + groupBy on the dashboard glass) — the engine and API are complete and gate-proven at the API level; the sliced KPI carries the UI proof this slice | D6 ruled (d) | next dashboard touch | Open |
| Line-level segments beyond PO lines (PR lines, Invoice lines, ASN lines) — the engine supports any type (SegmentApplication.LineLevel); PO lines are the one proof surface this slice, others adopt at their own gates with their own line-id plumbing | D6 Step 0(b) ruled | per-surface gates | Open |
| Registry rows for the four SYSTEM segments (making Department/Location/Category/Job filterable as segments on PR views) — deliberately NOT added this slice (native columns already filter; one key, one field, no rival). Revisit at the convergence slice | D6 ruled (a) | convergence slice | Open |
| Expression language for entry-form defaults (beyond static values + the @-token date grammar) — no consumer named one; rule 6 holds | D7 Step 0(d) ruled | gate-driven | Open |
| Per-form SERVER write semantics for displayType (e.g. rejecting writes to disabled/readOnly fields) — deliberately NOT claimed at D7: hidden ≠ forbidden, the role matrix is the authorization boundary; revisit only if a ruling names a concrete need | D7 OD-D7-2 boundary | operator ruling | Open |
| PO DISPLAY-forms (fields visible/hidden per role on the read surface) join the PoScreens EntityPage migration at its own gate — the D7 gate proved role forms on the honest entry consumer (PrForm) per OD-D7-1(ii) | D7 OD-D7-1 | PoScreens migration gate | Open |
| Numbering schemes for system-artifact prefixes (GRN, BID, AWD, VU, USR, FORM, DASH, VIEW) — stay literal this slice; extend NumberingScheme coverage when SPSB names one as a configurable document number | D7 OD-D7-6 | SPSB pull | Open |
| EntryFormDef.RecordType widening beyond Requisition — one row per entry surface as it migrates (RfqBuilder, ASN create, Invoice create, vendor-side forms); each gate adds its write-DTO-derived controllable set to EntryFormVocabulary | D7 OD-D7-5 | per-surface gates | Open |
| Onboarding queue ViewPicker mount — deferred at D7.5 (status-tab model, low value now); mounts with the useViewRows adapter whenever the queue's workflow warrants it | D7.5 Step 0(a) | onboarding touch | Open |
| Custom-list value delete had no in-use guard (GAP-5 / July AN-6) — a stored code silently degraded to free-text | Audit #2 | A2F | ✅ Done — A2F-T3 (c5b62c7): referenced values deactivate (reversible via the existing update path), unreferenced hard-delete; zero migrations (Active predated the slice) |
| AUTHZ-1: org-wide aggregate metrics reachable by vendor principals (spend x3 + vendorCount rode own-record read actions) | Audit #2 | A2F | ✅ Done — A2F-T1 (a55d08c): A72 ViewSpendAnalytics + vendorCount on A22; RoleMetricScopingTests |
| vendorsOnboardedSwecByMonth / NonSwecByMonth still gate on ViewVendors — org-wide onboarding-count series reachable by vendors, the same species AUTHZ-1 fixed for the four audited metrics (T1 completion-note flag; scope discipline held) | A2F-T1 finding | operator ruling | ✅ Done — A2F.1-T1: both series repointed to the internal roster action (A22, the vendorCount home); leak class fully closed — ZERO org-wide metrics reachable by role V |
